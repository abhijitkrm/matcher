[package]
name = "fuzzgen"
version = "0.1.0"
edition = "2021"
description = "Seeded adversarial command-stream generator for differential fuzzing"
license = "MIT OR Apache-2.0"

[dependencies]

[profile.release]
lto = "fat"
codegen-units = 1
